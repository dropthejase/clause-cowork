export const FONT = {
  label: 10,   // uppercase section headers (TAGS, TYPES, RELATED CLAUSES)
  sm: 11,      // body text, descriptions, pills, metadata
  md: 12,      // item titles, prominent labels
  title: 13,   // app/panel headings
  lg: 14,      // icon-adjacent buttons (×, +/−, send)
} as const;
